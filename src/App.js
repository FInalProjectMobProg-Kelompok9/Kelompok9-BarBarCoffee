import React from 'react'
import { StyleSheet, Text, View } from 'react-native'
import { HomeScreen, SplashScreen, MenuScreen } from './pages'

const App = () => {
  return (
      //<SplashScreen/>
      //<HomeScreen/>
      <MenuScreen/>
  )
}

export default App

const styles = StyleSheet.create({})
